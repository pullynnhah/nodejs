<div class="product">
  <a href="/products/{{this.id}}">
    <img src="{{this.thumbnail}}" />
    <h2>{{this.title}}</h2>
    <p>{{this.description}}</p>
    <strong>$ {{this.price}}</strong>
  </a>
</div>